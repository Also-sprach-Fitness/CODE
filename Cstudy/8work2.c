#include <stdio.h>
#include <ctype.h>
void print_blank(int blank);
int main(void)
{
    int rows,cols;
    int blank = 0;
    char ch;

    printf("Please enter some text:");
    while((ch=getchar()) != EOF)//读取输入
    {
        if (isblank(ch))
        {

        }
    }
//格式
//打印字符并处理特殊字符
}
void print_blank(int blank)
{

}